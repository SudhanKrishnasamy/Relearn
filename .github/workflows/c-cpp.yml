name: relearn

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: make check
      run: make run -C 3_Implementation/version2 
    - name: make distcheck
      run: make distcheck
